# 项目进度交接

## 已完成什么

- 项目名：`notion-knowledge-garden`，本地目录固定为 `F:\文档\个人笔记网站`。
- GitHub 仓库：`ningyan1228/notion-knowledge-garden`。
- 前端继续部署在 GitHub Pages，当前访问域名：`https://notes.101921.xyz`。
- API 代理部署在腾讯云服务器，接口域名：`https://knowledge-api.gjsx.uno`。
- 服务器代理目录：`/home/ubuntu/projects/knowledge-garden-proxy`。
- 网站已经接入 Notion 数据库，可以读取公开笔记、详情内容、分类、标签、封面、学习时长。
- 已实现网站内写笔记、编辑笔记、分类筛选、标签筛选、搜索、排序、分页加载。
- 已实现日记 / 笔记 / 灵感 / 复盘类型区分，日记默认偏私密展示。
- 已实现知识星图、热力成长地图、今日记录、今日待整理、拾光日记等模块。
- 已改成账号登录模式：
  - `zhiwu`
  - `juanjuan`
- 多用户规则已设计并写入前后端逻辑：
  - 公开且不是日记的内容，两个账号都可以看到。
  - 私密内容只能作者自己看到。
  - 日记只能作者自己看到。
  - 只能编辑自己的笔记。
- 本次已开始整理 UI：
  - 首页工作台从杂乱卡片流改成更清晰的分区布局。
  - 修复知识星图全屏时画布尺寸不正确的问题。
- 本次继续处理：
  - 修复网站读取封面时优先显示 Notion 页面顶部封面，导致新上传的数据库封面看起来不更新的问题。
  - 将 `主题地图 / 标签云 / 最近生长 / 随机漫游` 进一步降级成低矮探索栏，减少首页红框区域的杂乱感。
  - 将 `最近生长` 改为 `待回看`，避免和公开笔记列表重复。
  - 修复正文和封面图片上传容易失败的问题：前端会在上传前自动压缩大图，降低被 nginx 上传体积限制拦截的概率。
  - 将笔记列表从 `公开笔记` 改为 `我的笔记`，新增 `全部笔记 / 公开 / 私密` 可见性筛选。未勾选“公开到网站”的内容，登录作者账号后可在 `我的笔记 -> 私密` 中查看。
  - 删除首页红框区域里的 `主题地图 / 标签云 / 待回看 / 随机漫游` 四个低价值辅助模块，替换为按更新时间展示的 `更新脉络` 时间线。
  - 进一步简化 `今日待整理`：删除右侧 `写笔记 / 随机看 / 知识地图` 重复按钮，把内部布局改成“本周摘要 + 两列清单”。
  - 将顶部工作台继续调整为浅色 iOS grouped card 风格：白色半透明卡片、软圆角、细分隔线、列表行式信息，去掉大面积黑灰面板。
  - 将首页 hero 从左右双栏改为上下结构：上方是欢迎/写笔记入口，下方是全宽知识星图，减少横向拥挤。
  - `我的笔记` 的可见性筛选新增 `他人公开`，用于只查看当前登录用户之外的其他用户公开笔记。
  - 正文编辑器新增格式工具栏：加粗、引用、列表、代码块、H1/H2/H3 标题、绿色/蓝色/红色/黄色/紫色文字。
  - 后端 Markdown 解析新增富文本能力：`**加粗**`、行内代码、`{green:文字}` / `{blue:文字}` 等颜色标记会保存为 Notion 富文本。
  - 分类新增 `日记`；新建日记或切换类型为日记时，分类会自动选中 `日记`。
  - 写作体验继续增强：正文新增实时预览、本机自动保存草稿、日记模板、快捷插入日期/分割线/待办。
  - 待办语法 `- [ ] 任务` / `- [x] 任务` 会在预览、网页详情中显示为待办行，并在保存到 Notion 时转成 `to_do` 块。

## 当前卡在哪里

- Notion 数据库需要确认这 3 个字段已经存在：
  - `作者`：富文本
  - `用户ID`：富文本
  - `可见性`：选择，选项为 `公开`、`私密`
- 之前已经用 Notion 插件成功补过一次字段；如果保存仍报缺字段，再去 Notion 数据库里手动核对。
- 当前 UI 已做多轮收敛，但还需要上传到 GitHub Pages 后在真实页面看效果。
- 封面更新修复需要上传服务器端文件并重启代理后才会生效。
- 图片压缩修复只需要上传前端 `public/assets/app.js` 后强刷即可生效。

## 下一步要做什么

- 上传这次修改过的前端文件到 GitHub 仓库：
  - `public/index.html`
  - `public/assets/app.js`
  - `public/assets/styles.css`
  - `progress.md`
- 上传这次修改过的服务器文件：
  - `server/knowledge-garden-proxy/server.js`
- 服务器文件上传到：
  - `/home/ubuntu/projects/knowledge-garden-proxy/server.js`
- 然后在服务器执行：
  ```bash
  cd ~/projects/knowledge-garden-proxy
  docker compose up -d --build
  ```
- 上传后打开 `https://notes.101921.xyz`，按 `Ctrl + F5` 强制刷新。
- 检查首页是否更清爽，重点看：
  - 今日拾光区域是否不再过高。
  - 今日待整理是否更安静，并呈现浅色 iOS 分组列表的质感。
  - 首页顶部是否已经变成上下两段，知识星图是否更宽、更不拥挤。
  - 首页红框区域是否已经替换为 `更新脉络` 时间线，点击时间线条目是否能打开笔记详情。
  - `我的笔记` 是否可以切换 `公开 / 私密`，未公开笔记是否能在私密筛选下看到。
  - `我的笔记 -> 他人公开` 是否只显示其他用户公开到网站的笔记。
  - 编辑正文时，格式工具栏和 `Ctrl+B`、`Ctrl+Alt+1/2/3` 是否正常插入格式标记；保存后 Notion 和网页详情是否能显示加粗/颜色。
  - 写作弹窗里实时预览是否随正文同步变化；关闭再打开是否能恢复未发布草稿；日记模板、日期、分割线、待办按钮是否正常插入。
  - 知识星图全屏后节点是否正常出现。
- 重新编辑一篇已有笔记，选择新封面图，保存后刷新列表和详情页，确认封面变成新图。
- 再分别测试一次封面图片和正文图片上传；大图应先显示“图片已压缩，正在上传到 Notion...”。

## 2026-07-10 本轮功能更新

- 笔记管理升级：新增只看我的、他人公开、作者、月份、文件夹、收藏等组合筛选；“我的笔记”可进入独立笔记库模式，避免需要穿过首页长内容才能浏览列表。
- 阅读页升级：封面式标题区、作者与阅读时长、目录高亮、阅读进度、代码复制、相关笔记与上下篇导航；编辑按钮仅对笔记作者显示。
- 权限修复：前端和服务端均按 `用户ID` 判断归属；旧笔记会按已配置的作者名映射归属，跨账号保存会被服务端以 403 拒绝。
- 写作体验：默认可视化编辑，保留 Markdown 高级模式；格式、九种文字颜色、图片、待办、日记模板、草稿恢复均兼容 Notion 同步。
- 收藏与文件夹：已实现 Finder 风格文件夹架、我的收藏、文件夹数量、创建/筛选文件夹、写作时归档、笔记详情页直接移动到文件夹。文件夹归属和收藏会同步到 Notion。
- 首页收敛：压缩“朝夕拾光”和知识星图高度；星图会等待有效尺寸后绘制，失败时自动重试并支持手动重试。

### 已更新的 Notion 数据库字段

- 数据库：`个人知识网站`
- `文件夹`：选择
- `收藏`：复选框

### 当前发布清单

- 前端：`public/index.html`、`public/assets/app.js`、`public/assets/styles.css`、`progress.md`
- 服务端：`server/knowledge-garden-proxy/server.js`
- 服务端更新后执行：
  ```bash
  cd ~/projects/knowledge-garden-proxy
  docker compose up -d --build
  ```
- 前端发布后在 `https://notes.101921.xyz` 按 `Ctrl + F5` 强制刷新。

### 2026-07-10 独立页面导航改造

- 首页默认只展示完整的知识星图；侧边栏的“今日 / 我的笔记 / 拾光日记 / 成长热力 / 知识星图”改为独立视图切换，不再把所有模块纵向堆叠在同一页。
- 从知识星图点击分类或标签时，会自动切换到“我的笔记”并保留已选筛选条件；“返回首页”和“知识星图”都会回到完整星图画布。
- 本轮只涉及前端：发布 `public/index.html`、`public/assets/app.js`、`public/assets/styles.css` 与本文件即可，服务器无需操作。

### 2026-07-10 文件夹工作区强化

- “我的笔记”顶部的文件夹区域升级为两层 Finder 风格工作区：标题、收藏与新建入口在上方；下方以大卡片展示全部文件夹与自定义文件夹，每张卡片显示对应的笔记篇数。
- 自定义文件夹卡片新增重命名与删除：重命名会同步更新其中笔记的“文件夹”字段；删除只会将其中笔记移至未归档，不会删除笔记内容。
- 文件夹支持多级子目录：在文件夹卡片上点击“+”可创建子文件夹；重命名或删除父目录会递归同步子目录与对应笔记。
- 子目录树默认折叠，点击文件夹卡片右下角箭头才展开；文件夹面板限定自身最大高度并内部滚动，避免子目录增多把整页拉长。
- 文件夹区域重构为固定高度的 Finder 式目录树：不再堆叠文件夹卡片，目录层级以缩进树展示，文件夹数量增多时仅在树内滚动；右下角箭头改为进入/展开子目录，面包屑支持返回上层。
- 文件夹入口进一步收拢为现代“文件夹切换器”：笔记页只保留当前文件夹、收藏和新建操作；层级目录改为按需打开的浮层，避免文件管理区域抢占主页面空间。
- 文件夹展开与切换符号改为 macOS Finder 风格的细线 chevron（右侧展开、下方展开），移除字符式箭头图标。
- 成长热力改为内容自适应收拢的独立页面，移除热力图下方由外层应用容器造成的大块白色空白。
- 成长热力页面重排为紧凑的年度仪表盘：标题说明横向排版、三项统计一行呈现、热力图成为主体并放大单元格；独立视图的侧栏不再强制撑高整个页面。
- 新增笔记本 favicon，浏览器标签页与收藏夹会显示网站的专属笔记图标。
- 侧边栏新增全局“写笔记”按钮，可从任意独立页面直接打开写作弹窗。
- 侧边栏新增“写日记”按钮，直接打开日记类型的编辑器并沿用日记模板与私密默认值。
- 在“拾光日记”下新增独立的月历页：显示每天记录数量与日记标记，可切换月份；点击日期可打开当天唯一日记或继续写当天日记。
- 知识星图优化可读性：减少同时显示的笔记节点，普通笔记标题改为悬停显示；分类与标签的标题更大、更高对比，并拉开节点间距。
- 星图笔记节点进一步调整为最近 10 篇且始终显示深色短标题标签，避免出现无法辨认含义的空白节点；悬停仍可查看完整标题。

### 2026-07-11 文件空间独立页

- 文件夹已从“我的笔记”顶部完全移出，侧边栏新增独立的“文件空间”页面；笔记页只保留组合筛选与笔记列表。
- 文件空间采用独立的 Finder 风格工作区：顶部显示当前路径、收藏与新建入口，下方以固定高度、可滚动的树形目录管理多级文件夹，避免子目录增多把页面不断拉长。
- 选择某个文件夹会自动带着该文件夹筛选跳转到“我的笔记”；新建子文件夹、重命名、删除仍在文件空间内完成，操作图标改为线性图标。
- 本轮只涉及前端：发布 `public/index.html`、`public/assets/app.js`、`public/assets/styles.css` 与本文档即可，服务器无需操作。

### 2026-07-11 Finder 文件空间重做

- 文件空间不再使用顶部宣传卡和下方目录树的拼接布局，改为占满独立页面的 Finder 式窗口。
- 结构调整为：原生工具栏（返回、当前目录、新建）+ 左侧收藏与文件夹侧栏 + 右侧当前目录的文件夹图标网格；目录层级增长只在侧栏内滚动，不会撑高页面。
- 文件夹图标支持单击选中、双击进入、顶部返回上层；右侧保留“查看此文件夹笔记”，并支持新建子文件夹、重命名、删除。

### 2026-07-11 阅读详情与移动反馈

- 笔记详情页“移动到文件夹”成功后，按钮会短暂显示“已移动 ✓”，下方同时展示绿色成功反馈和目标文件夹；失败时展示红色错误反馈。
- 修复详情页可能长期停在“正在打开这片花园…”：前端详情请求增加 12 秒超时，Notion 服务端请求增加 15 秒超时。出现网络或 Notion 卡顿时会结束加载并给出回退内容/错误，而不是无限空白等待。
- 本项包含前端和服务器端更新：前端发布 `public/index.html`、`public/assets/app.js`、`public/assets/styles.css`，并上传 `server/knowledge-garden-proxy/server.js` 后重建代理服务。
- 代理健康检查现在返回 `build: 2026-07-11-detail-loading-fix`，可用于确认运行中的容器确实是本次详情加载修复版本。

### 2026-07-12 macOS 风格统一

- 应用外壳重做为更接近 macOS 的材质窗口：系统灰半透明侧栏、原生红黄绿窗口点、紧凑列表导航、系统蓝选中态与扁平工具栏。
- 知识星图从暗色照片海报改为明亮的网格画布；节点、连线和标签统一为 macOS 系统蓝/橙/紫/灰，阅读对比更清楚，主页面不再混用深色海报风格。
- 本轮只需发布前端 `public/assets/app.js`、`public/assets/styles.css` 与本文档；服务器无需操作。

### 2026-07-12 Finder 原生层级精修

- 去除首页“卡片套卡片”的网页仪表盘结构：顶部改为真正的窗口工具栏，知识星图直接使用主内容区，保留单一的内容画布层级。
- 侧栏加入简洁的自绘线性图标，并重新压缩品牌区、导航行、按钮与账号卡片的间距和圆角，视觉接近 Finder/备忘录而非营销页面。
- 星图移除开发者网格，替换为更细腻的点阵画布和柔和聚焦光晕；工具栏、节点和图例继续沿用系统蓝、橙、紫、灰的层级语义。
- 本轮需发布 `public/index.html`、`public/assets/styles.css` 与本文档；服务器无需操作。

### 2026-07-12 Personal Knowledge OS 组件重构

- 按 Apple Personal Knowledge OS 方向拆分了组件结构：Sidebar 增加产品身份区、`空间 / 知识` 导航分组和更轻的 iOS Settings 选中态；不再只是修改原页面颜色。
- 知识星图改为 `graph-workspace`：左侧为空间化玻璃画布，右侧为“今日洞察”组件，动态呈现阅读时长、新增笔记和新想法。
- 图谱节点系统重做：中心节点是带副标题的 Personal Knowledge OS 玻璃卡，分类为圆角胶囊，标签和普通笔记降级为更轻的层级；连接线降低默认存在感。
- 新增右下角 AI 整理浮层。当前提供不依赖密钥的本地归档建议（分类、标签、文件夹）；如需真正大模型对话，后续需要接入 AI API 和密钥。
- 本轮需发布 `public/index.html`、`public/assets/app.js`、`public/assets/styles.css` 与本文档；服务器无需操作。
- 修复知识图谱 Header 的文字重叠：标题区与说明/操作区改为响应式两列流式布局，删除绝对定位；窄屏会自动折为上下两行。

### 2026-07-12 第二轮 OS 化精修（优先级 1-3）

- 新增统一 Theme Tokens：背景、表面、文本、蓝色交互、成功/警告状态、分隔线、圆角和阴影全部收敛为同一套变量；并加入 `prefers-reduced-motion` 支持和全局 `focus-visible` 焦点样式。
- App Shell 收敛：Sidebar 固定为 228px，顶部工具栏增加当前页面名称、说明和可用的全局搜索（Enter 或 Cmd/Ctrl+K）；账户区改为紧凑的用户/同步状态入口，不再显示大型 Notion Sync 卡。
- 今日页：移除顶部四张独立统计卡，改为“今日拾光”标题下基于真实数据更新的紧凑指标；工作台减少嵌套卡片与浓重渐变。
- 我的笔记：筛选改为搜索优先，默认只展示搜索、类型、排序和“更多筛选”；附加筛选收进可展开浮层，列表在大屏 3 列、中屏 2 列、小屏 1 列。
- 拾光日记：无公开日记时改为正式空状态和“写一篇日记”入口；成长轨迹与文件空间同步收敛卡片高度、统计与 Finder 网格比例。
- 记录日历：新增“月历 + 当日详情”双栏，右侧显示所选日期的真实记录和新建日记/笔记入口；桌面宽度不足时自动折为纵向布局。
- 知识星图：洞察面板改为真实笔记数据计算，取消深色大面板；图谱工具移动至画布右上角，AI 入口缩为圆形浮动按钮并避免长期遮挡内容。

### 2026-07-12 布局稳定性修复

- 停止继续扩展视觉组件，先收束多轮 CSS 覆盖造成的布局回归。
- `今日` 页面改为唯一、正常文档流中的网格：今日拾光、今日记录、今日待整理、更新时间线各自拥有固定网格区域，取消会让待整理面板覆盖主卡的跨列/跨行冲突。
- `成长轨迹` 的浅色 Hero 明确使用深色正文、绿色年份和灰色说明，修复早期深色 Hero 白字规则泄漏导致的低对比文字；热力图仍保留现有数据和结构。
- 保留知识星图的现有节点、颜色与布局算法，仅固定画布与洞察面板的响应式网格，避免右侧面板或 AI 入口挤压主画布。
- 侧边栏未重做，仅使非选中项 hover 保持轻量，选中状态由路由的 `is-active` 唯一控制。
- 本轮只需发布前端 `public/assets/styles.css` 与本文件；服务器无需操作。

#### 补充：成长轨迹 Hero 文字对比

- 为避免旧主题变量或伪元素遮罩重新覆盖浅色 Hero，标题、年份与说明改为明确的深色/绿色/灰色色值，并禁用该区域的伪元素遮罩。

### 2026-07-12 成长轨迹与详情加载修复

- 成长轨迹热力图调整为“月份标签 + 格子”紧凑双列轨道；零记录格保留可见的中性灰，避免深色覆盖环境下只剩少量有记录的格子。
- 三项年度统计改为标签在上、数字在下的等宽指标，数值字号收敛，避免与文字挤在同一基线上。
- 笔记详情增加 9 秒界面兜底与请求编号：接口卡住、关闭详情或快速切换笔记时都不会保留“正在打开这片花园…”。
- 服务端详情读取增加 10 秒总时限，超时返回 504；健康检查版本更新为 `2026-07-12-detail-observability-fix`，用于确认线上容器已正确重建。

#### 补充：详情请求可观测性与缓存刷新

- `index.html` 的前端脚本地址加入版本参数，发布后浏览器会强制获取带有详情兜底的最新 `app.js`，避免 CDN 或旧标签页继续运行缓存脚本。
- 代理会在容器日志中记录详情请求的开始、成功（含区块数量与耗时）、未找到与错误状态；不记录令牌或正文内容。
